IO.puts ("Using operator '/' 10/2 => : #{10/2}")
# Using operator / to divide two numbers, always return a float.
# To return a Integer division, uses a division function -> div().

# Float numbers are 64-bit double precision.
# Can uses round/trunc to get a integer part of float.

IO.puts ("Using div(10,2) => #{div(10,2)}")
IO.puts ("Using div 10,2 => #{div 10,2}")

# To return the division remain, uses rem()

IO.puts ("Using rem 10,3 => #{rem 10,3}")
IO.puts ("Using rem(10,3) => #{rem(10,3)}")

IO.puts ("\nElixir supports entering binary, octal and hexadecimal numbers.") 
# Elixir supports entering binary, octal and hexadecimal numbers.

IO.puts ("Inserting 0b1010 => #{0b1010}")
IO.puts ("Inserting 0o777 #{0o777}")
IO.puts ("Inseritng 0x1F => #{0x1F}")
