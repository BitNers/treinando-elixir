# Elixir supports Boolean (True/False)
# There a bunch of predicate functions to check the value type
# 	Uses is_boolean() to check if a value is a Boolean or not.

IO.puts ("Elixir supports boolean, to check if a value is a Boolean, uses function 'is_boolean(variable)'.")

IO.puts ("Using is_boolean(true) => #{is_boolean(true)}")
IO.puts ("Using is_boolean(1) => #{is_boolean(1)}")

IO.puts ("\nThere's another functions such as 'is_integer()', 'is_float()', 'is_number()'")
